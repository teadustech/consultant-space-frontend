# Backend API URL - REQUIRED for production (e.g. Vercel). Use either:
# REACT_APP_API_URL (full base, e.g. https://your-backend.vercel.app/api)
# REACT_APP_BACKEND_URL (host only, e.g. your-backend.vercel.app — https and /api added automatically)
# Leave both unset for local dev (uses proxy to localhost:5000).
REACT_APP_API_URL=
# REACT_APP_BACKEND_URL=

# Optional: Analytics and monitoring
REACT_APP_GA_TRACKING_ID=your-google-analytics-id
REACT_APP_SENTRY_DSN=your-sentry-dsn

# Optional: Feature flags
REACT_APP_ENABLE_CHATBOT=true
REACT_APP_ENABLE_PAYMENTS=true
REACT_APP_ENABLE_GOOGLE_MEET=true
